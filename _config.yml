# Minimal Jekyll configuration for GitHub Pages

# Site settings
title: "KNDL 000"
description: "Fragments on technology, art, culture, philosophy — and the quiet noise of life."
author: "KNDL"
baseurl: ""
# URL absoluta del sitio en producción (canonical, Open Graph, robots.txt). GitHub Pages:
# url: "https://TU-USUARIO.github.io"   o dominio propio: https://ejemplo.com
locale: es_ES
# Imagen por defecto para redes (opcional; ruta bajo el sitio, ej. /assets/media/og.jpg)
# seo_image:

# Google Analytics 4 (ID en analytics.google.com → Administración → flujos de datos)
analytics:
  ga4_measurement_id: "G-2VWGMP1T5M"

# Build settings
markdown: kramdown
kramdown:
  hard_wrap: false
theme:
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Include admin directory for Tina CMS
include:
  - admin

# Collections configuration
collections_dir: content/collections

collections:
  images:
    output: true
    permalink: /imagenes/:name/
  poems:
    output: true
    permalink: /poemas/:name/
  vestigios:
    output: true
    permalink: /vestigios/:name/
  melange_reports:
    output: true
    permalink: /melange-reports/:name/
  fragmentos:
    output: false
  spotify_playlists:
    output: false
  youtube_setlists:
    output: false
  personal_music:
    output: false
  codigos:
    output: true
    permalink: /codigo/:name/

defaults:
  - scope:
      path: "content/collections/_posts"
    values:
      published: true
  - scope:
      type: melange_reports
    values:
      layout: melange-report
